# Enable the arc-rebar dracut module (installed by sys-kernel/dracut-arc-rebar).
# It runs a pre-udev hook that sets Resizable BAR on Intel discrete GPUs before
# xe/i915 binds the card. Remove this file (or the package) to disable.
add_dracutmodules+=" rebar "
